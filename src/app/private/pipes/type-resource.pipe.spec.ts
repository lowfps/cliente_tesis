import { TypeResourcePipe } from './type-resource.pipe';

describe('TypeResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
